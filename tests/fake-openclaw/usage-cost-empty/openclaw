#!/bin/sh
# the meter did not answer: nothing on stdout, non-zero exit.
# jq reads an empty stream, COST comes back empty, and an unguarded trip-wire calls that a pass.
echo 'Gateway unreachable.' >&2
exit 1
